{
  "name": "jarvis-desktop",
  "version": "0.3.1",
  "private": true,
  "description": "Jarvis desktop shell (Electron) — a rich CLIENT that loads the live Hub UI. Intentionally OUTSIDE the npm workspace so it never touches the Hub/runner install or CI (mirrors mobile/).",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "icon": "electron scripts/make-icon.mjs",
    "dist": "electron-builder",
    "doctor": "node -e \"try{console.log('electron', require('electron/package.json').version)}catch{console.log('electron: not installed — run npm install')}\""
  },
  "devDependencies": {
    "electron": "^34.0.0",
    "electron-builder": "^25.0.0"
  },
  "optionalDependencies": {
    "electron-updater": "^6.3.0"
  }
}
