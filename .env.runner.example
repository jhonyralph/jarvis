# Copy to `.env` (cp .env.runner.example .env) for docker-compose.runner.yml.
# Generate the token on the Hub:  jarvis machine "Sandbox"   (prints JARVIS_HUB + JARVIS_TOKEN)

JARVIS_HUB=wss://your-hub-host/
JARVIS_TOKEN=paste-the-runner-token-here
JARVIS_LABEL=Sandbox
