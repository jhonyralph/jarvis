# deps
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# build / distributables
dist/
build/
*.tsbuildinfo
jarvis-dist.tgz
*.tgz
*.apk
*.aab
*.ipa
*.xcarchive
*.xcarchive/

# local runtime data / models / audio (never commit — huge, and local-only by design)
*.wav
*.onnx
*.onnx.json
*.pt
*.bin
models/
data/

# env / secrets
.env
.env.*
!.env.example
!.env.runner.example
*.keystore
*.jks
*.p12

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# worktrees efêmeros criados por agentes em background (Agent tool isolation)
.claude/worktrees/

# log de `npm test` redirecionado para arquivo (regenerável; já vazou pro working tree uma vez)
testout.txt
