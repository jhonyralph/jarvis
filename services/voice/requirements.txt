# Local voice service — everything runs offline on this machine.
piper-tts>=1.4.0        # TTS (working)
# faster-whisper>=1.0   # STT (next phase; also needs ffmpeg on PATH)
# openwakeword          # wake word ("Jarvis") (later)
